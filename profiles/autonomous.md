# Autonomous Profile

The agent may inspect files, edit code, run tests, and fix discovered issues
that are directly required by the task.

The agent must stop before destructive operations, production changes,
credential changes, commits, pushes, or scope expansion that changes the user's
intent.
