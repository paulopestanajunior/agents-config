# GitHub Copilot Adapter

Copilot consumes generated user instructions from
`adapters/copilot/instructions/`.

Run `scripts/generate-adapters.sh` before installing or updating the harness.
Generated Copilot files are derived from canonical global components so adding a
new skill, role, workflow, profile, or rule does not require editing this
adapter manually.
