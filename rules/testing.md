# Testing Rules

- Run the most relevant validation for the changed surface.
- Add regression tests for bug fixes when practical.
- Run lint and type checks when the project already has them and the change
  touches code they cover.
- Do not claim a change is validated if tests or checks were not run.
- If validation cannot run, state the reason and the remaining risk.
